<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
                 
<mapper namespace="com.humuson.push2u.push.dao.PushDao">

    <!-- 앱 유저 리스트 가져오기 -->
	<select id="getAppUserList" resultType="java.util.Map">
		SELECT CUST_ID, PHONE_NUM
		FROM APP_USERS;	    
	</select>
	
	<!-- 캠페인 정보 INSERT -->
	<insert id="insertCampaign" parameterType="java.util.Map">
	    INSERT INTO push_campaigns (USER_ID, MSG_TYPE, PUSH_TITLE, POPUP_CONTENT, PUSH_MSG, INAPP_CONTENT, REQ_DATE, SMS_YN)
		VALUES (#{userId}, #{msgType}, #{pushTitle}, #{popupContents}, #{pushMsg}, #{inAppcontents}, SYSDATE(), #{smsYN});
	</insert>
    
</mapper>